# script_data

CVAT に投入するデータ、CVAT からダウンロードしたアノテーション、学習・評価用データセットの変換に関わるスクリプトを置きます。

Examples:

- `clip_frame.py`: 動画からフレーム抽出
- `count_head_annotations.py`: CVAT zip 内の Head アノテーション数を集計
- `convert_cvat_annotations.py`: CVAT XML を評価用 JSON に変換
